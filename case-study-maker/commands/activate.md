---
name: activate
description: Initialize case study tracking for the current project. Creates .case-study/ directory structure and links to Git history.
---

Run the `activate-case-study-maker` skill workflow.

Create `.case-study/` with `events.json`, `media/`, and `output/`. Add `.case-study/` to `.gitignore`. Read recent Git history and confirm activation.
